{
  "name": "kanbot",
  "private": true,
  "version": "1.0.0",
  "description": "Kanbot - Kanban multi-projeto com board master unificado",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix web install",
    "dev": "concurrently -n server,web -c yellow,cyan \"npm --prefix server run dev\" \"npm --prefix web run dev\"",
    "dev:server": "npm --prefix server run dev",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web run build",
    "start": "npm --prefix server start",
    "desktop:dev": "tauri dev",
    "desktop:build": "tauri build",
    "icons": "node src-tauri/icons/generate.mjs"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2.8.4",
    "concurrently": "^9.1.0"
  }
}
